% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/system_info.R
\name{keep_awake}
\alias{keep_awake}
\title{keep_awake}
\usage{
keep_awake(on = TRUE)
}
\arguments{
\item{on}{logical. \code{TRUE} holds the assertion, \code{FALSE} releases it.}
}
\value{
\code{TRUE} if an assertion is held after the call, otherwise \code{FALSE},
invisibly
}
\description{
Prevent the machine from going to sleep during a long computation, and allow
it again afterwards.

This exists because a system sleep is invisible to every in-process
measurement. While the machine is asleep wall clock advances and CPU time
does not, so a step that spans a sleep appears many times slower while doing
identical work: recorded pipeline timings have been inflated 5-14x this way,
with CPU-seconds, memory and I/O counters all looking entirely normal.

\strong{Nothing in Giotto holds a sleep assertion unless you call \code{keep_awake()} or
\code{\link[=gwith_awake]{gwith_awake()}}.} Attaching the package changes no power settings.
}
\details{
Uses \code{caffeinate} on macOS and \code{systemd-inhibit} on Linux; on other platforms,
or when neither is available, it reports that it cannot help and does nothing.
No sleep assertion can outlive the R session: on macOS the helper is bound to
this process and exits with it.

Idempotent — calling it repeatedly with \code{on = TRUE} leaves a single helper.
}
\section{Options}{

\describe{
\item{\code{giotto.prevent_sleep}}{when \code{FALSE}, \code{keep_awake()} and
\code{\link[=gwith_awake]{gwith_awake()}} become no-ops (default \code{TRUE}). This is a kill switch for
shared machines, cluster nodes and CI, where holding a power assertion is
unwanted; it does \strong{not} cause anything to hold one on its own.}
}
}

\examples{
\dontrun{
keep_awake(TRUE)
# ... long pipeline ...
keep_awake(FALSE)
}
}
\seealso{
\code{\link[=gwith_awake]{gwith_awake()}} to scope it to a block of code
}
